<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:installLocation="auto"
    android:versionCode="1"
    android:versionName="1.0">

    <!-- Require OpenGL ES 3.2 support. -->
    <uses-feature
        android:glEsVersion="0x00030002"
        android:required="false" />

    <!-- Require Vulkan 1.0.3 support (Quest 3 primary graphics API). -->
    <uses-feature
        android:name="android.hardware.vulkan.version"
        android:version="0x00400003"
        android:required="true" />

    <!-- Meta Quest 3 - Horizon OS v81 - 6DOF tracking -->
    <uses-feature
        android:name="android.hardware.vr.headtracking"
        android:version="1"
        android:required="true" />

    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:hasCode="false"> <!-- There is no Java or Kotlin code in the application. -->

        <!-- Quest metadata: Hand tracking support -->
        <meta-data android:name="com.oculus.handtracking.frequency" android:value="HIGH" />
        <meta-data android:name="com.oculus.handtracking.version" android:value="V2.0" />

        <!-- Supported devices -->
        <meta-data android:name="com.oculus.supportedDevices" android:value="quest3|quest2|questpro" />

        <!-- Chapter 3 doesn't use input - allow launch without controllers -->
        <meta-data android:name="com.oculus.vr.focusaware" android:value="true" />

        <activity
            android:name="android.app.NativeActivity"
            android:configChanges="screenSize|screenLayout|orientation|keyboardHidden|keyboard|navigation|uiMode"
            android:excludeFromRecents="false"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen">

            <!-- Tell NativeActivity the name of our .so. -->
            <meta-data
                android:name="android.app.lib_name"
                android:value="openxr_tutorial_ch3" />

            <!-- Standard Khronos OpenXR launcher intent filter. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="org.khronos.openxr.intent.category.IMMERSIVE_HMD" />
            </intent-filter>

            <!-- Meta Quest-specific non-standard intent filter. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="com.oculus.intent.category.VR" />
            </intent-filter>
        </activity>
    </application>

</manifest>
